[
  {
    "question": "Which commands should be used for ACLs on Niagara?",
    "answer": "On Niagara, you should use `mmputacl`, `mmgetacl`, `mmdelacl`, and `mmeditacl` for GPFS."
  },
  {
    "question": "Which commands are used for ACLs on Cedar or Graham?",
    "answer": "On Cedar and Graham, `setfacl` and `getfacl` are used for ACLs, as these systems use Lustre."
  },
  {
    "question": "Why are different ACL commands used on Niagara compared to Cedar/Graham?",
    "answer": "Different ACL commands are used because Cedar/Graham utilize Lustre, while Niagara uses GPFS, each requiring its native commands (`setfacl`/`getfacl` for Lustre, `mmputacl`/`mmgetacl`/`mmdelacl`/`mmeditacl` for GPFS)."
  },
  {
    "question": "Where can I find more information on `mmputacl` or `mmgetacl`?",
    "answer": "More information on `mmputacl` or `mmgetacl` can be found in their respective man pages or IBM documentation."
  },
  {
    "question": "Is there a way to recursively add or remove ACL attributes using GPFS commands?",
    "answer": "Yes, a sample bash script is available to recursively add or remove ACL attributes using GPFS built-in commands."
  }
]