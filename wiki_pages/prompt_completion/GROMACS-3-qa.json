[
  {
    "question": "What modules are required to run GROMACS version 2016.5 on CPUs within the `StdEnv/2016.4` environment?",
    "answer": "To run GROMACS 2016.5 on CPUs in `StdEnv/2016.4`, the following modules are required: `StdEnv/2016.4 gcc/6.4.0 openmpi/2.1.1 gromacs/2016.5`."
  },
  {
    "question": "Which modules are necessary to run GROMACS version 2016.5 with GPU acceleration in the `StdEnv/2016.4` environment?",
    "answer": "To run GROMACS 2016.5 on GPUs in `StdEnv/2016.4`, the necessary modules are: `StdEnv/2016.4 gcc/6.4.0 cuda/9.0.176 openmpi/2.1.1 gromacs/2016.5`."
  },
  {
    "question": "What are the compilation notes for GROMACS 2016.5 when used with `StdEnv/2016.4`?",
    "answer": "GROMACS 2016.5, when used with `StdEnv/2016.4`, is compiled with GCC and FFTW."
  },
  {
    "question": "How do you load GROMACS 2016.3 for CPU usage under `StdEnv/2016.4`?",
    "answer": "Load `StdEnv/2016.4 gromacs/2016.3` to use GROMACS 2016.3 on CPUs in `StdEnv/2016.4`."
  },
  {
    "question": "Which CUDA version is used for GROMACS 2016.3 with GPU support in `StdEnv/2016.4`?",
    "answer": "CUDA 8.0.44 is used for GROMACS 2016.3 with GPU support in `StdEnv/2016.4`."
  },
  {
    "question": "What compilation notes apply to GROMACS 5.1.5 in the `StdEnv/2016.4` environment?",
    "answer": "GROMACS 5.1.5 in `StdEnv/2016.4` is compiled with Intel and MKL."
  },
  {
    "question": "What modules are used for GROMACS 5.1.4 with GPU acceleration in `StdEnv/2016.4`?",
    "answer": "The modules for GROMACS 5.1.4 with GPU acceleration in `StdEnv/2016.4` are `StdEnv/2016.4 cuda/8.0.44 gromacs/5.1.4`."
  },
  {
    "question": "How is GROMACS 5.0.7 configured for CPU execution in `StdEnv/2016.4`?",
    "answer": "GROMACS 5.0.7 for CPU execution in `StdEnv/2016.4` uses `StdEnv/2016.4 gromacs/5.0.7`."
  },
  {
    "question": "What modules enable GROMACS 4.6.7 for CPU execution using GCC, MKL, and ThreadMPI in `StdEnv/2016.4`?",
    "answer": "The modules required are `StdEnv/2016.4 gcc/5.4.0 openmpi/2.1.1 gromacs/4.6.7`."
  },
  {
    "question": "What is the CUDA version for GROMACS 4.6.7 when compiled with GCC, MKL, and ThreadMPI for GPUs in `StdEnv/2016.4`?",
    "answer": "CUDA 8.0 is used for GROMACS 4.6.7 when compiled with GCC, MKL, and ThreadMPI for GPUs in `StdEnv/2016.4`."
  },
  {
    "question": "What bug is present in GROMACS versions 2020.0 to 2021.5 when using GPUs of Volta or newer generations?",
    "answer": "GROMACS versions 2020.0 up to and including 2021.5 contain a bug that could have perturbed the virial calculation and led to incorrect pressure coupling when used on GPUs of Volta or newer generations with the `mdrun` option `-update gpu`."
  },
  {
    "question": "Under what condition does the GPU bug in GROMACS 2020.0-2021.5 manifest?",
    "answer": "The GPU update is not enabled by default, so the error can only appear in simulations where the `-update gpu` option was manually selected."
  },
  {
    "question": "Which GROMACS versions are compiled for the `StdEnv/2020` software environment?",
    "answer": "GROMACS versions 2020.4 and newer have been compiled for the new `StdEnv/2020` standard software environment."
  },
  {
    "question": "What compilers and libraries are used for GROMACS versions 2018.7 and newer?",
    "answer": "GROMACS versions 2018.7 and newer have been compiled with GCC compilers and the MKL-library to run a bit faster."
  },
  {
    "question": "How were older GROMACS versions typically compiled?",
    "answer": "Older versions were compiled with either GCC compilers and FFTW or Intel compilers, using Intel MKL and Open MPI 2.1.1 libraries from the default environment."
  },
  {
    "question": "Are CPU (non-GPU) versions of GROMACS available in both single and double precision?",
    "answer": "Yes, CPU (non-GPU) versions are available in both single- and double precision, with the exception of GROMACS 2019.3, where double precision is not available for AVX512."
  }
]